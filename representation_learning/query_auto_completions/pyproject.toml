[project]
name = "query-auto-completions"
version = "0.1.0"
description = "Search Intention Network for Query Auto-Completion"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0.0",
    "pytorch-lightning>=2.0.0",
    "datasets>=2.14.0",
    "click>=8.0.0",
    "black>=24.10.0",
]
